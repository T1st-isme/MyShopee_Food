@model IEnumerable<Shopee_Food.Models.ThanhToan>

@{
    ViewBag.Title = "Index";
}
<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.STK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PTTT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.TaiKhoan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.STK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PTTT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.TaiKhoan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTT }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTT })
        </td>
    </tr>
}

</table>
