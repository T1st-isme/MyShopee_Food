@model Shopee_Food.Models.ThanhToan

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>ThanhToan</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.STK)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.STK)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.PTTT)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PTTT)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.User.TaiKhoan)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.User.TaiKhoan)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
